# 核心依赖（Python 3.9+）
# 基础功能无需额外依赖，只使用标准库

# 可选依赖：Redis 支持
# 如果要使用 RedisTaskQueue，需要安装：
# redis>=4.5.0

# 开发依赖
# mypy>=1.0.0           # 类型检查
# pytest>=7.0.0         # 单元测试
# pytest-asyncio>=0.21  # 异步测试

