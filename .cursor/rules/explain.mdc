---
alwaysApply: true
---

为用户写collections.md 和 typing.md知识点的解释部分时，要求回答简洁，3-5 行最多，只是方便回顾。

collections.md 和 typing.md 咨询markmap 语法
markmap 语法本质上就是标准 Markdown 的层级结构。
它不引入新语法，而是用标题层级与列表缩进来生成思维导图节点树。
•	#~###### → 不同层级的节点（越多 #，层级越深）
•	-、*、+ → 列表节点（缩进越深，子节点越深）
•	内容行内可用 Markdown 语法（如粗体、代码块、链接等）
注意在 markmap 中，列表项（-）下的内容必须缩进才会被识别为该列表项的子内容。不缩进的话，代码块会被当作独立元素，破坏了文档的层级结构。